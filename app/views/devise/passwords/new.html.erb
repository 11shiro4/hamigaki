<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>パスワードをわすれたひと</title>
  <link href="https://fonts.googleapis.com/css2?family=DotGothic16&display=swap" rel="stylesheet"> <!-- ドットゴシックフォントを読み込み -->
  <style>
    body {
      display: flex;
      flex-direction: column;
      align-items: center;
      height: 100vh;
      font-family: 'DotGothic16', sans-serif; /* ドットゴシックフォントを適用 */
      text-align: center;
      margin: 0;
      padding-top: 50px; /* 追加: 上に余白を追加 */
      overflow: hidden; /* エレメントのはみ出しを隠す */
    }

    header {
      background-color: #ffc107; /* パステルイエロー */
      text-align: center;
      padding: 20px;
      width: 100%;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      position: fixed; /* ヘッダーをページの上部に固定 */
      top: 0;
      left: 0;
      right: 0;
    }

    header h1 {
      margin: 0;
      font-size: 2rem;
      color: #fff;
    }

    .form-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
      margin-top: 150px; /* 修正：フォームの位置を少し下げる */
    }

    .field {
      margin-bottom: 15px;
      display: flex;
      justify-content: center;
    }

    .field label {
      display: block;
      width: 100%;
    }

    .field input {
      display: block;
      width: 100%;
      max-width: 300px; /* 修正：入力欄を中央寄せ */
    }

    .actions {
      text-align: center;
    }

    .actions input[type="submit"] {
      background-color: #ff69b4; /* ホットピンク */
      color: white;
      border: none;
      border-radius: 20px;
      padding: 10px 20px;
      font-size: 1em;
      cursor: pointer;
      transition: background-color 0.3s ease, transform 0.3s ease;
    }

    .actions input[type="submit"]:hover {
      background-color: #ff85c2; /* 少し明るいピンク */
      transform: scale(1.1); /* マウスオーバーで大きくなる */
    }

    .back-button {
      position: absolute;
      left: 20px;
      top: 20px;
    }

    footer {
      background-color: #ffd54f; /* パステルイエロー */
      text-align: center;
      padding: 10px;
      color: white;
      position: absolute;
      bottom: 0;
      width: 100%;
    }

    /* dentist-day.png を右から左へ歩かせるアニメーション */
    @keyframes walk {
      0% { transform: translateX(100vw); }
      100% { transform: translateX(-100px); }
    }

    .walking-image {
      position: absolute;
      bottom: 150px; /* 画像の位置を少し上に */
      width: 50px;
      height: 50px;
      animation: walk 10s linear infinite;
    }
  </style>
</head>
<body>
  <header>
    <h1>パスワードをわすれたひと</h1>
  </header>

  <%= link_to new_kid_session_path, class: "back-button" do %>
    <%= image_tag 'arrows.png', alt: '戻る', width: 40, height: 40 %>
  <% end %>

  <div class="form-container">
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <div class="field">
        <%= f.label :email, "メールアドレス" %><br />
        <%= f.email_field :email, autofocus: true %>
      </div>

      <div class="actions">
        <%= f.submit "パスワードをリセット" %>
      </div>
    <% end %>
  </div>

  <img src="<%= asset_path('dentist-day.png') %>" alt="歩く画像" class="walking-image">

  <footer>
    <p>© 2025 シャカシャカにっき All Rights Reserved.</p>
  </footer>
</body>
</html>