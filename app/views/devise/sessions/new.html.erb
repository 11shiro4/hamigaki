<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ログイン</title>
  <link href="https://fonts.googleapis.com/css2?family=DotGothic16&display=swap" rel="stylesheet"> <!-- ドットゴシックフォントを読み込み -->
  <style>
    body {
      display: flex;
      flex-direction: column;
      font-family: 'DotGothic16', sans-serif; /* ドットゴシックフォントを適用 */
      text-align: center;
      margin: 0;
      height: 100vh;
    }

    header {
      background-color: #ffc107; /* パステルイエロー */
      text-align: center;
      padding: 20px;
      width: 100%;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      flex: 0 0 auto; /* 固定の高さを維持 */
    }

    header h1 {
      margin: 0;
      font-size: 2rem;
      color: #fff;
    }

    .main-content {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      flex: 1; /* 中央に配置するためのフレックス要素 */
      padding: 20px;
      position: relative; /* 要素の相対位置 */
    }

    .form-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
    }

    .field {
      margin-bottom: 15px;
      display: flex;
      justify-content: center;
    }

    .field label {
      display: block;
      width: 100%;
    }

    .field input {
      display: block;
      width: 100%;
      max-width: 300px; /* 修正：入力欄を中央寄せ */
    }

    .actions {
      text-align: center;
    }

    .actions input[type="submit"] {
      background-color: #ff69b4; /* ホットピンク */
      color: white;
      border: none;
      border-radius: 20px;
      padding: 10px 20px;
      font-size: 1em;
      cursor: pointer;
      transition: background-color 0.3s ease, transform 0.3s ease;
    }

    .actions input[type="submit"]:hover {
      background-color: #ff85c2; /* 少し明るいピンク */
      transform: scale(1.1); /* マウスオーバーで大きくなる */
    }

    .links {
      text-align: center;
      margin-top: 10px;
    }

    footer {
      background-color: #ffd54f; /* パステルイエロー */
      text-align: center;
      padding: 10px;
      color: white;
      flex: 0 0 auto; /* 固定の高さを維持 */
    }

    /* dentist-day.png のバウンスアニメーション */
    @keyframes bounce {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-20px); }
    }

    .walking-image {
      position: absolute; /* 相対的に固定位置にする */
      bottom: 10%; /* 画像の位置を調整 */
      left: 10px; /* 左側に配置 */
      width: 50px;
      height: 50px;
      animation: bounce 2s infinite;
    }

    @media screen and (min-width: 600px) {
      .walking-image {
        left: 15%; /* 大きな画面では少し内側に */
      }
    }
  </style>
</head>
<body>
  <header>
    <h1>ログイン</h1>
  </header>

  <div class="main-content">
    <img src="<%= asset_path('dentist-day.png') %>" alt="バウンス画像" class="walking-image">
    <div class="form-container">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <!-- 上から二つ目のログイン見出しを削除 -->
        <div class="field">
          <%= f.label :email, "メールアドレス" %><br />
          <%= f.email_field :email, autofocus: true %>
        </div>
        <div class="field">
          <%= f.label :password, "パスワード" %><br />
          <%= f.password_field :password, autocomplete: "off" %>
        </div>
        <div class="actions">
          <%= f.submit "ログイン" %>
        </div>
      <% end %>
    </div>

    <div class="links">
      <%= render "devise/shared/links" %>
    </div>
  </div>

  <footer>
    <p>© 2025 シャカシャカにっき All Rights Reserved.</p>
  </footer>
</body>
</html>