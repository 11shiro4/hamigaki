<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>hamigaki</title>
  <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/assets/style.css">

  <style>
    .icon-container {
      display: flex;
      justify-content: center; /* 中央ぞろえ */
      gap: 10px; /* アイコンの間隔 */
      flex-wrap: wrap; /* 横並び */
    }

    .icon-option {
      display: flex;
      align-items: center;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
      cursor: pointer;
      text-align: center;
      justify-content: center;
    }

    .icon-option input {
      display: none;
    }

    .icon-preview {
      width: 40px; /* アイコンのサイズを調整 */
      height: 40px;
      margin-right: 5px;
    }

    .icon-label {
      font-size: 1.0em; /* ボタンのラベルのサイズを調整 */
    }

    .btn-primary {
      font-size: 1.2em; /* "OK"ボタンのサイズも調整 */
      padding: 10px 20px;
      cursor: pointer;
      background-color: #007bff;
      color: white;
      border: none;
      border-radius: 5px;
      margin-top: 10px; /* 上部に余白を追加 */
    }
  </style>
</head>
<body>
  <header>
    <h1>hamigakiへようこそ！</h1>
  </header>

  <main>
    <section class="title">
      <h2>あたらしいhamigakiのきろくをついか</h2>
    </section>
    <%= form_with(model: [current_kid, BrushingLog.new], local: true) do |form| %>
      <div>
        <% current_time = Time.current.in_time_zone('Asia/Tokyo') %>
        <p>日時: <%= current_time.strftime("%Y/%m/%d(%a)%H:%M") %></p>
        <%= form.hidden_field :brushed_at, value: current_time %>
      </div>

      <div>
        <%= form.label :icon_type, "じょうずにできた？" %>
        <div class="icon-container">
          <label class="icon-option">
            <%= form.radio_button :icon_type, :perfect %>
            <%= image_tag "perfect.png", alt: "パーフェクト", class: 'icon-preview' %>
            <span class="icon-label">パーフェクト</span>
          </label>

          <label class="icon-option">
            <%= form.radio_button :icon_type, :good %>
            <%= image_tag "good.png", alt: "よくできた", class: 'icon-preview' %>
            <span class="icon-label">よくできた</span>
          </label>

          <label class="icon-option">
            <%= form.radio_button :icon_type, :done %>
            <%= image_tag "done.png", alt: "できた", class: 'icon-preview' %>
            <span class="icon-label">できた</span>
          </label>
        </div>
      </div>

      <div>
        <%= form.submit "OK", class: "btn-primary" %>
      </div>
    <% end %>
  </main>

  <footer>
    <p>© 2025 hamigaki All Rights Reserved.</p>
  </footer>
</body>
</html>